<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="action_send_mail_on_delivery_completed_prac" model="ir.actions.server">
            <field name="name">Send Mail to Salesperson on Delivery Completed</field>
            <field name="model_id" ref="stock.model_stock_picking"/>
            <field name="state">code</field>
            <field name="code">
if (record.picking_type_id.code == 'outgoing' and record.sale_id and record.sale_id.user_id and record.sale_id.user_id.partner_id.email):
    mail_template = env.ref('gautam_prac_odoo18.mail_template_delivery_notification_prac')
    comment_subtype = env.ref('mail.mt_comment').id

    record.with_context(
        force_send=True,
        default_email_to=record.sale_id.user_id.partner_id.email,
    ).message_post_with_source(
        mail_template,
        subtype_id=comment_subtype,
        email_layout_xmlid='mail.mail_notification_light',
    )

</field>
        </record>
        <record id="auto_send_mail_delivery_completed_prac" model="base.automation">
            <field name="name">Auto Email Salesperson on Delivery Completed</field>
            <field name="model_id" ref="stock.model_stock_picking"/>
            <field name="trigger">on_write</field>
            <field name="trigger_field_ids" search="[('model_id.model','=','stock.picking'),('name','in',['state'])]"/>
            <field name="filter_pre_domain">[]</field>
            <field name="filter_domain">[("state", "=", "done"), ("picking_type_id.code", "=", "outgoing")]</field>
            <field name="action_server_ids" eval="[(6, 0, [ref('action_send_mail_on_delivery_completed_prac')])]"/>
            <field name="active">True</field>
        </record>

    </data>
</odoo>
